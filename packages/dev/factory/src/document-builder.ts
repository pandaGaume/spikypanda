/**
 * In the core since 2026-09-21 (`spikypanda-core`, `document/document.builder`);
 * re-exported here so the demo's generators keep their imports.
 */
export { buildDocument, buildDocumentJson, validateDocumentSpec, DocumentBuildError } from "spikypanda-core";
export type { DocumentNodeSpec, DocumentConnectionSpec, DocumentTileSpec, IDocumentProblem } from "spikypanda-core";
