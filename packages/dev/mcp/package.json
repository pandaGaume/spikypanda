{
    "name": "@spiky-panda/mcp",
    "version": "0.1.0",
    "description": "Model Context Protocol behaviors and adapters that expose the SpikyPanda node editor and research store to scientific agents",
    "private": true,
    "type": "module",
    "main": "dist/browser-index.js",
    "module": "dist/browser-index.js",
    "types": "dist/browser-index.d.ts",
    "files": ["dist", "bundle", "grammars", "src"],
    "scripts": {
        "build": "npm run clean && npm run compile",
        "clean": "rimraf dist bundle && rimraf tsconfig.build.tsbuildinfo tsconfig.research.tsbuildinfo",
        "compile": "npm run compile:browser && npm run compile:research",
        "compile:browser": "tsc -b tsconfig.build.json",
        "compile:research": "tsc -b tsconfig.research.json",
        "watch:browser": "tsc -b tsconfig.build.json -w",
        "bundle": "webpack --mode production --config webpack.config.cjs",
        "bundle:dev": "webpack --mode development --config webpack.config.cjs"
    },
    "sideEffects": false,
    "dependencies": {
        "@cyanmycelium/mcp-core": "*"
    },
    "devDependencies": {
        "rimraf": "~6.0.1",
        "ts-loader": "^9.5.0"
    }
}
